---
description: Start the preview server and test the project
---
Run the following commands to start the preview server:

1. Run the Next.js development server:
// turbo
```bash
powershell -ExecutionPolicy Bypass -Command "npm run dev"
```

2. Open the browser to the local URL (usually `http://localhost:9002` since it's hardcoded in package.json) and verify it's working.
